{
  "name": "surfshadow",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --experimental-strip-types --loader ./tests/ts-loader.mjs --test ./tests/background.test.ts",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^25.9.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.4.0"
  }
}
